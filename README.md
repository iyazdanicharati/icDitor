
# icDitor  
نسخه: 1.0.12-A-Final  
توسعه‌دهنده: Iman Yazdani Charati (iClassic)

icDitor یک ویرایشگر متن پیشرفته، سبک، بدون وابستگی (Vanilla JS) و کاملاً RTL‑Friendly است که با هدف ارائه یک ادیتور حرفه‌ای، سریع و منعطف برای پروژه‌های تحت وب توسعه داده شده است.  
این ادیتور هیچ وابستگی به کتابخانه‌های دیگر ندارد و کاملاً مستقل کار می‌کند.

---

## ✨ ویژگی‌های کلیدی

### 🔹 پشتیبانی کامل RTL / LTR  
کاملاً مناسب برای فارسی، عربی و سایر زبان‌های راست‌به‌چپ.  
امکان تغییر جهت پاراگراف تنها با یک کلیک.

### 🔹 کنترل حرفه‌ای اینتر (Enter Behavior)  
Mode A (پیش‌فرض):
- Enter → درج `<br>`
- Shift+Enter → درج `<div><br></div>`
- بدون هیچ خط اضافه یا گیر کردن کرسر

### 🔹 هدینگ Toggle (ویژه icDitor)  
H1 / H2 / H3 ↔ برگشت به P  
بدون تولید تگ‌های اضافی یا Nest اضافی.

### 🔹 شمارنده دقیق کاراکتر و خطوط  
خط‌ها دقیق شمرده می‌شوند حتی با `<br>`، `<div>` و متن چندسطری.

### 🔹 پلاگین‌های داخلی  
- درج جدول  
- درج لینک  
- حذف لینک  
- درج iframe  
- پاکسازی کامل استایل متن (Paste Clean)  
- رنگ متن و رنگ بک‌گراند  
- حالت Source View  
- ابزار شناور انتخاب سریع (Floating Toolbar)

### 🔹 فونت‌های سفارشی  
پشتیبانی از:
```html
<textarea data-fonts="IRANSans, Vazir, Yekan"></textarea>
```
فونت پیش‌فرض: **Tahoma**

### 🔹 ذخیره خودکار (Auto‑Save)  
با LocalStorage همگام‌سازی می‌شود و متن کاربر هرگز از بین نمی‌رود.

### 🔹 بدون وابستگی (Zero Dependencies)  
فقط JavaScript خام.  
بدون jQuery، بدون Bootstrap، بدون هیچ کتابخانه خارجی.

---

## 📦 نصب و راه‌اندازی

### ۱) فایل‌ها را اضافه کنید:
```html
<link rel="stylesheet" href="icditor.css" />
<script src="icditor.js"></script>
```

### ۲) المنت موردنظر را اضافه کنید:
```html
<textarea data-editor="true" data-count="true"></textarea>
```

### ۳) آماده است!  
icDitor به صورت خودکار textarea هایی که دارای  
`data-editor="true"`  
هستند را تبدیل می‌کند.

---

## ⚙️ آپشن‌ها (Attributes)

| نام | توضیح | نمونه |
|-----|-------|--------|
| `data-editor` | فعال‌کردن icDitor برای عنصر | `true` |
| `data-fonts` | معرفی فونت‌های سفارشی | `"Vazir, Yekan"` |
| `data-count` | فعال‌سازی شمارنده خطوط/کاراکتر | `true` |

---

## 🧩 رویدادها و ذخیره‌سازی  
icDitor همیشه مقدار نهایی را داخل **textarea اصلی** قرار می‌دهد.  
بنابراین فرم‌های HTML بدون مشکل ارسال می‌شوند.

---

## 🛠 توسعه  
کد ساختاریافته و ماژولار است.  
کلاس اصلی:
```
class icDitor { ... }
```

اگر خواستی نسخه ESModule یا TypeScript داشته باشی، فقط اعلام کن.

---

## 📜 لایسنس  
MIT License  
Free for personal + commercial use.

---

## ❤️ سازنده  
**Iman Yazdani Charati – iClassic Team**  
```
icDitor - Lightweight RTL Friendly Editor
```
```